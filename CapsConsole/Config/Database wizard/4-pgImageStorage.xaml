<PageFunction
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib" 
    xmlns:tx="clr-namespace:Tools.WindowsT.WPF.ConvertersT;assembly=Tools.Windows"
    x:Class="pgImageStorage"
    x:TypeArguments="sys:Boolean"
    xmlns:my="clr-namespace:Caps.Console"
    KeepAlive="True"
    WindowTitle="Image storage selection">
    <FrameworkElement.Resources>
        <my:OrBooleanConverter x:Key="OrBooleanConverter"/>
        <my:EnumInConverter x:Key="EnumInConverter"/>
      </FrameworkElement.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>

        <Label Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="1" Content="Image storage selection"/>

        <Label Grid.Row="2" Grid.Column="1" Visibility="{Binding DatabaseConnectionType, Mode=OneWay, Converter={StaticResource EnumInConverter}, ConverterParameter='Existing'}" Content="Image storage cannot be changed for existing database. Proceed to next step."/>
        <Grid Grid.Row="2" Grid.Column="1" Visibility="{Binding DatabaseConnectionType, Mode=OneWay, Converter={StaticResource EnumInConverter}, ConverterParameter='New, Empty'}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Content="Store cap images in database" Name="chkCapImages" IsChecked="{Binding CapImagesInDb, Mode=TwoWay}"/>
            <CheckBox Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" Content="Store other images in database" Name="chkOtherImages" IsChecked="{Binding OtherImagesInDb, Mode=TwoWay}"/>
            <Label Grid.Column="0" Grid.Row="2" Content="Image root" HorizontalAlignment="Right" VerticalAlignment="Center" Name="lblImageRoot">
                <Label.IsEnabled>
                    <MultiBinding Converter="{StaticResource OrBooleanConverter}" Mode="OneWay">
                        <Binding ElementName="chkCapImages" Path="IsChecked" Mode="OneWay"/>
                        <Binding ElementName="chkOtherImages" Path="IsChecked" Mode="OneWay"/>
                    </MultiBinding>
                </Label.IsEnabled>
            </Label>
            <TextBox Grid.Column="1" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Name="txtImageRoot" Text="{Binding ImageRoot, Mode=TwoWay}">
                <TextBox.IsEnabled>
                    <MultiBinding Converter="{StaticResource OrBooleanConverter}" Mode="OneWay">
                        <Binding ElementName="chkCapImages" Path="IsChecked" Mode="OneWay"/>
                        <Binding ElementName="chkOtherImages" Path="IsChecked" Mode="OneWay"/>
                    </MultiBinding>
                </TextBox.IsEnabled>
            </TextBox>
            <Button Grid.Column="2" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Name="btnImageRoot" Content="...">
                <Button.IsEnabled>
                    <MultiBinding Converter="{StaticResource OrBooleanConverter}" Mode="OneWay">
                        <Binding ElementName="chkCapImages" Path="IsChecked" Mode="OneWay"/>
                        <Binding ElementName="chkOtherImages" Path="IsChecked" Mode="OneWay"/>
                    </MultiBinding>
                </Button.IsEnabled>
            </Button>
        </Grid>

        <StackPanel Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="backButton" Click="backButton_Click" Width="50" xml:space="preserve">&lt; _Back</Button>
            <Button Name="nextButton" IsEnabled="True" Width="50" Margin="10,0,0,0" xml:space="preserve">_Next &gt;</Button>
            <Button Name="cancelButton" Click="cancelButton_Click" Width="50" Margin="10,0,0,0" IsCancel="True">_Cancel</Button>
            <Button Name="finishButton" Click="finishButton_Click" Width="50" Margin="10,0,0,0" IsDefault="True" IsEnabled="False">_Finish</Button>
        </StackPanel>

    </Grid>
</PageFunction>